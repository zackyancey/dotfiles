# Dotfiles

This dotfiles repo is set up to be used with the bare git repo method described
[here](https://www.atlassian.com/git/tutorials/dotfiles).

## Setup

To set up on a new machine, run:

```
curl -Lks https://git.io/Jejn3 | /bin/bash
```
